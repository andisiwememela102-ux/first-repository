START  
 SET Largest = 0 
   loop indefinely
 Display " Enter a positive number (0 to stop)
  Input Number
if user input = 0 then
 break
if largest number= None OR user input> largest number then
  largest number < user input 
END IF
 END loop

BEGIN
 //Prompt the user to enter their name
DISPLAY "Please enter your name"
  READ first_name

//combine the user's name with the greeting
Print "first_name" + "Hello World"

//Output the final message
DISPLAY greeting_message 
 END


//Initialize accumulators
Sum = 0
count = 0

//Read integers until no more input available loop
value - Read Next  Integer
if End of input ( value) then
break
endif

//update accumulators 
sum - sum + value
count - count + 1
  end loop

//compute average if at least one number was provided
if count > 0 then
 average - sum / count
 else 
 average - Undefined  //  Or 0, depending on requirements
 endif 

 //Return or display the result
 Return average


BEGIN
// Step 1. Read the full grocery list and store it in a list 
// Step 2. Sort the list in alphabetical order.
// Step 3. Initialize an empty container for items still to buy 
// Step 4. Interate through each product in the sorted list.
// Step 5. For each product, prompt the user to enter whether it has been bought or not.
// Step 6. If each product has not been bought, add it to the list of products that are still left to buy 
// Step 7. After intering through all the products, Print out the list of products that are still left to buy.
END


START
 Display X , Y
 
Calculate S = X + Y
Calculate A = S / 2
Calculate P = X * Y

OUTPUT S,A,P

STOP













 

 

